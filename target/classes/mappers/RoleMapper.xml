<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.onlineeducationplatform.mapper.RoleMapper">

    <select id="selectRoleNamesByUserId" parameterType="int" resultType="string"> SELECT r.name FROM
        roles r INNER JOIN user_roles ur ON ur.role_id = r.id WHERE ur.user_id = #{userId} </select>

    <select id="selectRoleIdByName" parameterType="string" resultType="int"> SELECT id FROM roles
        WHERE name = #{name} </select>

    <insert id="insertUserRole"> INSERT INTO user_roles (user_id, role_id) VALUES (#{userId},
        #{roleId}) </insert>

</mapper>